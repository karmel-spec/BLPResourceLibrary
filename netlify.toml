# Netlify configuration for pianotechnologylibrary.com
# Static site — no build step; the repo root is published as-is.

[build]
  publish = "."
  command = ""

# Long-cache the immutable model/thumbnail images.
[[headers]]
  for = "/thumbs/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Security + sensible defaults for the HTML/JS.
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "SAMEORIGIN"
    Referrer-Policy = "strict-origin-when-cross-origin"

# HTML must always revalidate — stale pages point at stale ?v= assets, which
# left users running old JS for days (the sign-in loop lingered this way).
# Assets themselves stay long-cached via their version params.
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

[[headers]]
  for = "/"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# Friendly URLs: /contributor/<id> → the profile page.
[[redirects]]
  from = "/contributor/*"
  to = "/contributor.html?id=:splat"
  status = 200
